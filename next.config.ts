const nextConfig = {
  /* config options here */
  turbopack: {
    // Root directory for Turbopack to resolve workspace, avoiding parent lockfile
    root: __dirname,
  },
};

export default nextConfig;
